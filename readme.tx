//npm install
//npm start (auto create Table "exchangemds")
******************
API (http://localhost:3000/exchangeData) to  store data in the database

*********************
API(http://localhost:3000/Exponentiamoving)


Calculate the 7-days moving average of each exchange rate, based on the closed exchange rate.

Calculate the 7-day exponential moving average of each exchange rate, based on the closed exchange rate.
